import 'package:flutter/material.dart';

class SharedVideoPostDisplayPost extends StatelessWidget {
  final postData;

  const SharedVideoPostDisplayPost({Key key, this.postData}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
