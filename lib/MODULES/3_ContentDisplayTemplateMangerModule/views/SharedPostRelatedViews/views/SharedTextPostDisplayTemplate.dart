import 'package:flutter/material.dart';

class SharedTextPostDisplayTemplate extends StatelessWidget {
  final postData;

  const SharedTextPostDisplayTemplate({Key key,@required this.postData}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
